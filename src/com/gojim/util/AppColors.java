package com.gojim.util;

import java.awt.Color;

/**
 * New theme inspired by your design:
 * - Deep black background
 * - Yellow accent (hero style)
 */
public class AppColors {

    // Main backgrounds (deep black style)
    public static final Color BACKGROUND = new Color(8, 8, 10); // almost pure black
    public static final Color CARD = new Color(18, 18, 22); // soft dark card
    public static final Color CARD_DARK = new Color(12, 12, 15); // sidebar

    // Text colors
    public static final Color TEXT_WHITE = new Color(245, 245, 245);
    public static final Color TEXT_GRAY = new Color(140, 140, 150);

    // 🔥 NEW ACCENT COLOR (Yellow from your image)
    public static final Color ACCENT = new Color(255, 215, 0); // GOLD / HERO YELLOW

    // Borders
    public static final Color BORDER = new Color(40, 40, 45);

    public static final Color BLACK = new Color(0, 0, 0);
}